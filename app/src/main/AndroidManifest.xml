<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.instantmusic.appmovil"
    >
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.INTERNET" />
    <supports-screens
        android:largeScreens="true"
        android:smallScreens="true"
        android:normalScreens="true"
        android:xlargeScreens="true" />
    <application

        android:allowBackup="true"
        android:icon="@drawable/unknown"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".main.MusicApp"
            android:screenOrientation="portrait"
            android:label="@string/app_name"
            tools:ignore="LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter></activity>
        <activity android:name=".main.Login"></activity>
        <activity android:name=".main.Recover"></activity>
        <activity android:name=".main.Regist"></activity>
        <activity android:name=".main.Search"></activity>
        <activity android:name=".main.Friends"></activity>
        <activity android:name=".main.Podcasts"></activity>
        <activity android:name=".main.Settings"></activity>
        <activity android:name=".song.SongActivity"></activity>
        <activity android:name=".playlist.Playlists"></activity>
        <activity android:name=".server.remoteServer"></activity>
        <activity android:name=".playlist.addPlaylist"></activity>
    </application>

</manifest>